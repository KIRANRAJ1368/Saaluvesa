# Saaluvesa Admin Panel

Run this dashboard with `npm run dev`. It uses the protected routes from the root `../backend` API and defaults to `http://localhost:5000/api`.

Override the endpoint with `VITE_ADMIN_API_URL` when deploying.
